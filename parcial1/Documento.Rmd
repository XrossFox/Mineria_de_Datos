---
title: "Parcial 1"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.


#Spam en lo comentarios de Youtube
##(Aqui van los nombres del equipo)
##Mineria de Datos
##(Nombre del profesor)


#Abstract
bla
bla
bla
bla
bla


#Contenido
indice
indice
indice

##Introduction
(Los data sets son sobre diferentes comentario en youtube. algunos son Spam y otros no)

##Problematic
(Diferenciar spam de comentarios reales)

#Exploratory Analysis

##(Descripcion de Variables, columnas)
##(Resumen estadistico simple)
##(Boxplots e interprestacion)
##(Sezgo de los datos. Interpretacion)
##(Histogramas. Interpretacion)
##(Cuartiles. Interpretacion)
##(Correlacion. Interpretacion)
Como el conjunto de datos, contiene dos atributos que no son numericas, primero necesitamos quitar dichas columnas:
```{r Correlacion}
#Con este comando se carga el data set
cpuData <- read.csv("machine.csv")

#Con los siguientes dos comandos, se eliminan las columnas no numericas, y se guardan en una matriz
cpuDataMat <- cpuData[-1]
cpuDataMat <- cpuDataMat[-1]

#Con este comando se hace la correlacion del dataset
cor(cpuDataMat)
```

##(Scatterplot. Interpretacion)

#Conclution